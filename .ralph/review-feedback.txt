
**Issue Found**: T021 claims completion but `src/audit/cli.py` does NOT exist.

**Details**:

1. **Missing File**: `src/audit/cli.py` was supposed to be created by T021 but doesn't exist in the filesystem.

2. **Git status confirms**:
   - `src/audit/model_evaluator.py` - modified
   - `src/audit/scorecard.py` - untracked (created)
   - `src/audit/cli.py` - **does not exist**

3. **Task T021 requirements** (from tasks.md):
   - Create `src/audit/cli.py` with AEGF header
   - Move all 6 subcommand functions (`cmd_sample`, `cmd_generate_exam`, `cmd_baseline`, `cmd_adapter`, `cmd_score`, `cmd_full`)
   - Move `_shared_parser`, `build_parser`, `main()`
   - Move `load_dotenv()` call into `main()` body
   - Preserve exact subcommand names and flags for FR-006 CLI compatibility

4. **Pre-existing test failure** (unrelated to this task): `tests/test_inference.py::TestGeminiClientWithMock::test_generate_with_retry_succeeds_after_one_failure` - TypeError with `tools` keyword argument.

**Action Required**: Create `src/audit/cli.py` as specified in T021 requirements.
